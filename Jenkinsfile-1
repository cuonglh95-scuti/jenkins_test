pipeline {
    // agent {
    //     docker { image 'node:16-alpine' }
    // }

    agent any

    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                sh 'docker info'
            }
        }
    }
}
